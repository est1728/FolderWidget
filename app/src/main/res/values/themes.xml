<resources xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="NewApi">

    <style name="Theme.FolderWidget" parent="Theme.MaterialComponents.DayNight.NoActionBar" />

    <!--
      ธีมสำหรับ popup โฟลเดอร์: พื้นหลังโปร่งใส ไม่มี title bar
      ปิด animation เปิด-ปิด activity มาตรฐาน (เราคุม animation เองด้วย
      overridePendingTransition + scale/fade เพื่อให้ดู "ผุด" ขึ้นมาเนียนๆ)
    -->
    <style name="Theme.FolderWidget.Popup" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@style/PopupWindowAnimation</item>
        <item name="android:backgroundDimEnabled">false</item>
        <!-- บังคับให้หน้าต่างยืดเต็มจอจริงๆ รวมพื้นที่ status bar / navigation bar / รอยบาก
             (แก้ปัญหาเห็นขอบบน-ล่างของโฮมสกรีนโผล่มา บนเครื่องที่รองรับ freeform window) -->
        <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="27">shortEdges</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <style name="PopupWindowAnimation" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/popup_enter</item>
        <item name="android:windowExitAnimation">@anim/popup_exit</item>
    </style>
</resources>
